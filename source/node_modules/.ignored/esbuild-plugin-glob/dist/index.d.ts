import chokidar from 'chokidar';
import * as esbuild from 'esbuild';

interface GlobPluginOptions<TControls extends boolean> {
    chokidarOptions?: chokidar.WatchOptions;
    /** Setting this to true returns a tuple with the plugin and a controls object */
    controls?: TControls;
    /** Disables all logging */
    silent?: boolean;
    additionalEntrypoints?: string[];
}
interface GlobPluginControls {
    /** Stops watching if in watch mode */
    stopWatching: () => Promise<void>;
}
declare type ReturnValue<TControls extends boolean> = TControls extends true ? [esbuild.Plugin, GlobPluginControls] : esbuild.Plugin;
declare function globPlugin<TControls extends boolean = false>({ chokidarOptions, controls, silent, additionalEntrypoints, }?: GlobPluginOptions<TControls>): ReturnValue<TControls>;

export { globPlugin };
